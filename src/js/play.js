import Human from "./human";

const person1 = new Human(200,180,50);


/*const w = window.innerWidth;
const h = window.innerHeight;

const canvas = document.getElementById("tablet");
canvas.setAttribute('width', w - 2);
canvas.setAttribute('height', h - 2);

const ctx = tablet.getContext('2d');

const modelOne = new Human(ctx,200,500,200);
let save = modelOne.draw();
let deg = 0;
let time;


modelOne.setKnee(90);
save = modelOne.draw(save);



time = setInterval(function(){
    modelOne.setKnee(deg);
    save = modelOne.draw(save);
    deg++;
    if (deg > 2)
        clearInterval(time);
},1000);*/

/*time = setInterval(function(){
    modelOne.setKnee(deg);
    save = modelOne.draw(save);
    deg--;
    if (deg < 1)
    clearInterval(time);
},400);*/
